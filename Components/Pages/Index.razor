@page "/"
@layout Journal_App.Components.Layout.EmptyLayout

@inject NavigationManager Nav

<p>Loading...</p>

@code {
    protected override void OnInitialized()
    {
        // Simple startup route: show login first.
        Nav.NavigateTo("/login", true);
    }
}
